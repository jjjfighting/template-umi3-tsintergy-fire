// =============================================================================
// 一些通用的固定样式
// =============================================================================

import { marginGap, marginWrapperGap } from './constant';

/**
 * 文本颜色
 */
export const styleTextColor: React.CSSProperties = {
  color: 'var(--neutral-color7)',
};

/**
 * 上边距 16
 */
export const styleMarginTop16: React.CSSProperties = {
  marginTop: marginGap,
};

/**
 * 右边距 16
 */
export const styleMarginRight16: React.CSSProperties = {
  marginRight: marginGap,
};

/**
 * 下边距 16
 */
export const styleMarginBottom16: React.CSSProperties = {
  marginBottom: marginGap,
};

/**
 * 左边距 16
 */
export const styleMarginLeft16: React.CSSProperties = {
  marginLeft: marginGap,
};

/**
 * 上边距 24
 */
export const styleMarginTop24: React.CSSProperties = {
  marginTop: marginWrapperGap,
};

/**
 * 右边距 24
 */
export const styleMarginRight24: React.CSSProperties = {
  marginRight: marginWrapperGap,
};

/**
 * 下边距 24
 */
export const styleMarginBottom24: React.CSSProperties = {
  marginBottom: marginWrapperGap,
};

/**
 * 左边距 24
 */
export const styleMarginLeft24: React.CSSProperties = {
  marginLeft: marginWrapperGap,
};
